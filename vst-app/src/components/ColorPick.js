import React, { Component } from 'react';
import { Button } from 'reactstrap';

export default class ColorPick extends Component {
    render() {
        return <Button onClick = {this.props.blackShirt} />
    }
}